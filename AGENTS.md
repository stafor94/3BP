# AGENTS.md

이 파일은 `stafor94/3BP` 저장소 전체에 적용되는 작업 규칙입니다.
모든 AI 에이전트, 자동화 도구, Codex/ChatGPT 작업 세션은 코드 수정 전에 이 규칙을 확인하고 따라야 합니다.

## 1. 작업 기본 원칙

- 작업 시작 시 반드시 최신 `main`을 기준으로 현재 상태를 확인한다.
- 문제의 원인을 먼저 확인하고, 요청 범위를 벗어나는 불필요한 수정은 하지 않는다.
- 사용자 동작을 변경하는 수정은 관련 회귀 테스트를 추가하거나 기존 테스트를 갱신한다.
- 완료 전 최소한 프로젝트가 요구하는 빌드/검증 절차를 실행하고 결과를 확인한다.
- 기존 CI 실패가 있으면 이번 변경으로 발생한 실패인지 기존 실패인지 구분해 보고한다.

## 2. 사용자-visible 품질 검증 — 필수 완료 조건

물리/시뮬레이션, 렌더링/VFX, 카메라/추적, 입력, 상호작용 UI처럼 **실제 실행 결과의 체감 품질이 작업 목표에 포함되는 변경**은 자동 테스트나 CI 통과만으로 완료 처리하지 않는다.

이 유형의 작업은 코드 수정 전에 반드시 `docs/AGENT_QUALITY_VALIDATION.md`도 읽고 따른다.

### 자동 검증과 실제 결과를 구분한다

- 회귀 테스트, 타입 검사, 빌드, CI는 correctness 검증이며 사용자-visible 품질의 최종 승인과 동일하지 않다.
- 요청한 현상을 실제 앱/시뮬레이션에서 재현하고 변경 후 결과를 직접 확인해야 한다.
- 사용자가 영상, 이미지, 재현 절차를 제공한 경우 해당 자료와 동일하거나 충분히 동등한 조건을 우선 검증 기준으로 사용한다.
- 실제 실행 결과를 확인하지 않은 상태에서 사용자-visible 품질 작업을 `완료`, `최종`, `해결됨`이라고 보고하지 않는다.

### 개선 작업은 baseline과 acceptance criteria를 먼저 잡는다

- `개선`, `자연스럽게`, `현실적으로`, `덜 어색하게`처럼 정성적인 요청은 구현 전에 관찰 가능한 작업별 acceptance criteria로 구체화한다.
- 기존 동작을 개선하는 작업은 가능하면 최신 `main`의 baseline과 변경 결과를 동일 조건에서 A/B 비교한다.
- 새 결과만 보고 에이전트의 주관으로 `좋아졌다`고 판정하지 않는다.
- 작업별 수치나 세부 목표는 Issue/PR/사용자 요청에 맞게 정하며, 검증 근거 없이 임의의 전역 임계값을 영구 규칙으로 만들지 않는다.

### 문제의 실제 계층을 우회하지 않는다

- 원인이 physics/state에 있는데 renderer clone, 화면 offset, CSS, VFX 같은 presentation-only 변경으로 가린 경우 이를 근본 해결로 보고해서는 안 된다.
- presentation-only 접근이 적절하다면 실제 simulation state 변경 여부와 화면 표현만 변경한 범위를 명확히 보고한다.
- 구현 난이도를 이유로 작업 도중 acceptance criteria를 임의로 낮추지 않는다. 충족하지 못한 항목은 미완료 또는 제한 사항으로 명시하며, 기준 변경은 사용자의 명시적 동의가 있을 때만 한다.

### 병렬 작업은 최종 integration에서 다시 검증한다

- 하나의 사용자 경험을 구성하는 강결합 기능을 여러 PR/브랜치로 병렬화한 경우 각 PR은 부분 작업으로 취급한다.
- 개별 PR의 테스트/CI 통과를 최종 품질 검증으로 대체하지 않는다.
- integration 담당자는 최신 `main` 기준에서 전체 acceptance criteria를 end-to-end로 다시 검증하고 실제 실행 결과를 확인한다.
- 통합 후 상호작용 때문에 추가 수정이 필요하면 integration 브랜치에서 조정한 뒤 다시 검증한다.

### 완료 상태를 정확히 표현한다

사용자 결과 확인이 필요한 작업은 다음 상태를 구분한다.

- **기술 검증 완료**: 구현, 자동 테스트, 빌드/CI, 에이전트가 수행 가능한 실제 실행 검증까지 끝난 상태
- **사용자 결과 확인 완료**: 사용자가 중요하게 보는 실제 결과물까지 확인되어 요청한 체감 품질이 충족된 상태

사용자 확인이 남아 있으면 기술 검증 완료를 최종 완료처럼 표현하지 않는다.

## 3. 버전 관리 — 필수 완료 조건

버전 관리는 선택 사항이 아니다. 사용자에게 보이는 앱 동작이 변경되는 모든 작업에서 반드시 확인해야 하는 **작업 완료 조건**이다.

현재 버전의 단일 기준(Source of Truth)은 루트 `package.json`의 `version`이며, 세부 정책은 `VERSIONING.md`를 따른다.

### 반드시 버전을 올려야 하는 변경

다음 중 하나라도 해당하면 버전을 올린다.

- 버그 수정
- UI/UX 변경
- 물리/시뮬레이션 동작 변경
- 렌더링 변경
- 카메라/추적/입력 동작 변경
- 사용자 설정 또는 저장 동작 변경
- 성능 개선 중 사용자 실행 결과에 영향을 주는 변경
- 새로운 기능 또는 의미 있는 기능 확장

### 버전 증가 기준

- 버그 수정, 작은 UI 수정, 성능 개선: **PATCH 증가**
- 하위 호환되는 새로운 기능 또는 의미 있는 기능 확장: **MINOR 증가**
- 호환되지 않는 큰 변경: `VERSIONING.md`의 0.x 정책에 따라 **MINOR 증가** 후 변경 내용을 명확히 기록

예: `0.18.9`에서 버그를 수정했다면 `0.18.10`으로 올린다.

### CHANGELOG 동시 갱신

버전을 올리는 모든 작업은 **같은 PR/작업 단위에서 `CHANGELOG.md`도 반드시 갱신**한다.

- `package.json`의 새 버전과 동일한 버전 항목을 `CHANGELOG.md` 최상단에 추가한다.
- 날짜를 기록한다.
- `Added`, `Changed`, `Fixed`, `Removed` 등 적절한 분류를 사용한다.
- 이미 배포한 버전의 내용을 덮어쓰지 않는다. 후속 수정은 반드시 새 버전으로 기록한다.

### 절대 금지

다음 상태에서는 작업을 완료했다고 보고하거나 PR을 병합하면 안 된다.

- 사용자-visible 코드가 변경됐는데 `package.json` 버전이 그대로인 상태
- 버전은 올렸는데 `CHANGELOG.md`에 동일 버전 항목이 없는 상태
- `package.json` 버전과 `CHANGELOG.md` 버전이 불일치하는 상태
- 이전에 배포한 버전 번호를 재사용하는 상태
- 코드만 먼저 병합하고 버전/CHANGELOG를 후속 PR로 처리하려는 상태

**코드 수정 + 테스트 + 버전 증가 + CHANGELOG 기록은 하나의 릴리스 단위로 처리해야 한다.**

### 버전을 올리지 않아도 되는 경우

앱 동작에 영향이 없는 아래 변경은 버전 상승을 생략할 수 있다.

- 문서만 수정
- 주석만 수정
- 개발자용 설명만 수정
- 사용자 동작에 영향을 주지 않는 테스트/CI 내부 정리

단, 애매하면 `VERSIONING.md`를 확인하고 사용자-visible 동작이 조금이라도 달라지면 버전을 올리는 쪽을 선택한다.

## 4. PR/병합 직전 최종 체크리스트

사용자-visible 변경을 포함한 PR은 병합 전에 아래 항목을 모두 확인한다.

- [ ] 변경 내용이 요청 범위와 일치한다.
- [ ] 필요한 회귀 테스트가 추가/수정되었다.
- [ ] 필수 빌드/테스트를 실행했다.
- [ ] 체감 품질 작업이면 작업별 acceptance criteria를 정의하고 확인했다.
- [ ] 체감 품질 작업이면 실제 실행 결과를 확인했다.
- [ ] 개선 작업이면 가능한 범위에서 최신 `main` baseline과 A/B 비교했다.
- [ ] 병렬 통합 작업이면 개별 PR이 아니라 최종 integration 결과를 end-to-end로 재검증했다.
- [ ] `package.json` 버전이 변경 성격에 맞게 증가했다.
- [ ] `CHANGELOG.md`에 동일한 새 버전 항목이 있다.
- [ ] 버전/CHANGELOG가 코드 변경과 **같은 PR**에 포함되어 있다.
- [ ] 기존에 배포한 버전 번호를 재사용하지 않았다.

해당되는 항목 중 하나라도 충족하지 못하면 작업은 미완료이며 병합하지 않는다.

## 5. 완료 보고 규칙

작업 완료 보고에는 최소한 아래 내용을 포함한다.

- 무엇을 수정했는지
- 어떤 테스트/검증을 통과했는지
- 체감 품질 작업이면 실제 실행에서 무엇을 어떤 조건으로 확인했는지
- 개선 작업이면 baseline 대비 무엇이 달라졌는지
- 사용자 결과 확인이 아직 필요한지 여부
- 사용자-visible 변경인 경우 새 버전 번호
- CHANGELOG 반영 여부
- PR 번호 또는 병합 커밋

버전 상승이 필요한 작업에서 버전 번호를 확인하지 않은 채 "완료"라고 보고해서는 안 된다.
실제 결과 확인이 필요한 작업에서 자동 테스트/CI만 근거로 "완료"라고 보고해서도 안 된다.
