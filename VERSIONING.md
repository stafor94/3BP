# Versioning Policy

이 프로젝트는 [Semantic Versioning 2.0.0](https://semver.org/)을 기준으로 버전을 관리합니다.
한국어 설명은 [SemVer 한국어 문서](https://semver.org/lang/ko/)를 참고합니다.

## 버전 형식

버전은 항상 `MAJOR.MINOR.PATCH` 형식을 사용합니다.

예: `0.1.11`, `1.4.2`, `2.0.0`

- **MAJOR**: 이전 버전과 호환되지 않는 변경
- **MINOR**: 이전 버전과 호환되는 새로운 기능 추가
- **PATCH**: 이전 버전과 호환되는 버그 수정

MINOR를 올리면 PATCH는 `0`으로 초기화하고, MAJOR를 올리면 MINOR와 PATCH를 모두 `0`으로 초기화합니다.

## 0.x 버전 정책

SemVer에서 `0.y.z`는 초기 개발 단계입니다. 이 구간에서는 공개 동작이나 구조가 안정적이라고 보장하지 않습니다.

3BP 프로젝트에서는 `1.0.0` 이전에 다음 기준을 사용합니다.

- 버그 수정, 작은 UI 수정, 성능 개선: **PATCH** 증가
  - `0.1.0 -> 0.1.1`
- 새로운 기능 또는 의미 있는 기능 확장: **MINOR** 증가
  - `0.1.11 -> 0.2.0`
- 초기 개발 단계의 호환되지 않는 큰 변경: **MINOR** 증가 후 변경 내용을 명확히 기록
  - `0.2.4 -> 0.3.0`
- 안정 버전으로 공개할 준비가 되었을 때: `1.0.0`

`1.0.0` 이후에는 SemVer 표준대로 호환되지 않는 변경 시 MAJOR를 증가시킵니다.

## 버전의 단일 기준

현재 버전의 단일 기준(Source of Truth)은 루트의 `package.json`에 있는 `version` 값입니다.

```json
{
  "version": "0.1.0"
}
```

웹 화면의 `3 Body Problem` 제목 옆 버전도 이 값을 직접 읽습니다. 화면 표시용 버전을 별도로 하드코딩하지 않습니다.

## 버전 올리는 방법

필요한 변경 종류에 따라 아래 명령을 사용합니다.

```bash
npm run bump:patch
npm run bump:minor
npm run bump:major
```

각 명령은 `package.json`의 버전만 올리며 Git commit이나 tag를 자동 생성하지 않습니다.

직접 수정하는 경우에도 반드시 유효한 `MAJOR.MINOR.PATCH` 형식을 사용합니다.

## 릴리스 절차

1. 변경 내용을 확인하고 PATCH / MINOR / MAJOR 중 하나를 결정합니다.
2. `package.json` 버전을 올립니다.
3. `npm run build`가 통과하는지 확인합니다.
4. 버전 변경과 해당 기능/수정을 같은 릴리스 단위로 `main`에 반영합니다.
5. 배포가 정상임을 확인한 뒤 가능하면 동일 버전의 Git tag `vMAJOR.MINOR.PATCH`를 생성합니다.

예:

```bash
git tag v0.2.0
git push origin v0.2.0
```

## 운영 원칙

- 이미 배포한 버전 번호를 다시 사용하지 않습니다.
- 같은 버전의 배포 내용을 나중에 덮어쓰지 않습니다. 수정이 필요하면 새 PATCH 이상을 발행합니다.
- 사용자에게 보이는 기능/동작이 변경되어 `main`에 배포될 때는 버전 증가 여부를 반드시 검토합니다.
- 문서나 주석만 변경되고 앱 동작에 영향이 없는 경우에는 버전을 올리지 않아도 됩니다.
- 정식 릴리스 전 버전이 필요하면 SemVer의 pre-release 표기법을 사용할 수 있습니다. 예: `1.0.0-beta.1`.

## 공식 기준

Semantic Versioning 2.0.0의 핵심 규칙은 다음과 같습니다.

- 호환되지 않는 API 변경: MAJOR 증가
- 하위 호환 기능 추가: MINOR 증가
- 하위 호환 버그 수정: PATCH 증가
- `0.y.z`는 초기 개발 단계
- 한 번 배포한 버전의 내용은 변경하지 않고 새 버전을 발행

공식 문서: https://semver.org/
